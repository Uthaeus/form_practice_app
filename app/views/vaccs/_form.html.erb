<%= form_with(model: vacc, local: true) do |form| %>
  <% if vacc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vaccination.errors.count, "error") %> prohibited this vaccination from being saved:</h2>

      <ul>
      <% vacc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :vacc_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>